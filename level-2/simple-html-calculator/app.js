const form = document.getElementById("form") //calculatorForm

console.log(form)
document.addEventListener('submit', function(event){
event.preventDefault()
console.log(form)
    let addOne = form.addOne.value
    let addTwo = form.addTwo.value
    // let sum = form.sum.value
    // form.addOne.value = ""
    // form.addTwo.value = ""
    // sum = addOne + addTwo
    this.alert("works")

    // if event is the add button then add
})

// define vars

// addition function
const add = () => {
    //return inputOne + inputTwo
}